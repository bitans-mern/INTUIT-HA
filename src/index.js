import React from 'react';
import ReactDOM from 'react-dom';
import InstantLoanApp from './components/InstantLoanApp';

ReactDOM.render(
  <React.StrictMode>
    <InstantLoanApp />
  </React.StrictMode>,
  document.getElementById('root')
);